version: "3.8"

services:

  app:
    build: .
    ports:
      - "8090:8080"
    networks:
      - s-network


  postgres:
    image: postgres:latest
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Rohit321
      POSTGRES_DB: global_booking
    ports:
      - "5433:5432"
    networks:
    - s-network


networks:
   s-network:
    driver: bridge

